fn square(value: i32): i32 {
    value * value
}

fn main() {
    let values = [1, 2, 3, 4];
    for value in values {
        println(value, " squared is ", square(value));
    }

    let add_ten = (value: i32) => {
        value + 10
    };
    println("closure result: ", add_ten(5));
}
